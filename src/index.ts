import * as Option from "./option-utils"
import * as Result from "./result-utils"

import { Option as OptionType, Some, None } from "./option"
import { Result as ResultType, Ok, Error } from "./result"

export { Option, OptionType, Some, None, Result, ResultType, Ok, Error }
